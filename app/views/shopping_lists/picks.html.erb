Your Shopping Lists Orders:

<!-- ADDING MAP WITH MARKS -- AND THE DISTANCE STILL MISSING ON THIS PAGE -->

<% @orders.each do |o| %>
<p>
  <p><%=  o.user.first_name %></p>
  <p><%=  o.user.location %></p>
  <p>Total items in the shopping list: <%=  o.items.count %></p>
</p>
<% if o.status %>
<%= link_to "Mark as Delivered!", order_delivered_path(o), method: :post, remote:true, class: "btn btn-ghost" %>
<% else %>
  <p>This order is delivered</p>
<% end %>
  <%= link_to "View Shopping List Details!", shopping_list_path(o.shopping_list), class: "btn btn-ghost" %>
<% end %>
